# ARCHITECTURE — устройство проекта

> Структура и все изменения. Обновляется по команде `git save`.
> Правила работы — в CLAUDE.md, его не дублируем, тут только карта и changelog.

## Назначение
Генератор сопроводительных писем под вакансии (аналитик данных Д. Кочетов)
с итеративной доработкой промпта по замечаниям пользователя.

## Слои промпта (приоритет: core > corrections > rules)
- `prompt/core.md` — заблокированное ядро. Меняется только командой «изменить core».
- `prompt/rules.md` — банк формулировок (термины + обороты), справочник стиля. Пометки
  `[факт]` (шаблон с цифрой — только под факт из digest) и `[фон]` (книжный термин, не дословно).
- `prompt/corrections.md` — правки пользователя (append-only, применяются только `active`).
- `prompt/humanizer.md` — флаг очеловечивания (одна строка: `on`/`off`, по умолч. `on`).

## Вход / выход
- `inputs/resume/` — резюме (`.md`), источник фактов №1. PDF не читаем (медленно/дорого).
- `inputs/jira/` — задачи Jira, источник опыта №2.
- `inputs/facts-digest.md` — выверенный дистиллят кейсов из резюме+Jira с цифрами и привязкой
  к источнику; основной рабочий источник фактов (чтобы не перечитывать сырую Jira). Исключён из git.
- `inputs/vacancy.md` — текущая вакансия.
- `outputs/letters.md` — варианты письма (плоская версия), по умолч. 2.
- `outputs/letters_humanized.md` — те же письма после скилла `humanizer` (когда флаг `on`).
- `outputs/letters_edit.md` — площадка «режима редакции»: пользователь правит письма прямо
  в редакторе, пишет `ок` — правки разбираются в правила.
- `bench/` — эталонные ПАРЫ вакансия+письмо для регресс-проверки. Одна папка на вакансию
  по короткому имени работодателя (`bench/gri/` = vacancy.md + letters.md).

## Служебные файлы
- `MEMORY.md` — журнал диалога: на чём закончили.
- `ARCHITECTURE.md` — этот файл.

## Команды
- «Сгенерируй письмо» (по умолч. 2 варианта + строка времени генерации), «Замечание: …»,
  «Правка письма / режим редакции» (правка в letters_edit.md → `ок` → правила),
  «Проверь на противоречия», «Повысить C-NNN до core», «humanizer on/off» — описаны в CLAUDE.md.
- `git save` — флаш + бэкап: (1) обновить MEMORY.md и ARCHITECTURE.md актуальным
  состоянием; (2) `git add -A` (inputs/jira исключён через .gitignore), commit, `git push origin main`.
  Репо: github.com/konicaRu/cover-letter-prompt (main, ПУБЛИЧНЫЙ). Перед коммитом —
  sanity-check, что Jira не в staged.
- `git load` — утреннее восстановление: прочитать MEMORY.md + active-правки + humanizer
  и кратко доложить, где остановились. Только чтение, ничего не пишет.

## Бэкап / git
- Удалённый репозиторий: `github.com/konicaRu/cover-letter-prompt`, ветка `main`, публичный.
- `.gitignore` исключает `inputs/jira/` и `inputs/facts-digest.md` (внутренние данные А3 — не публикуем).
- Прежнее решение «git не заводим» (2026-06-03) отменено 2026-06-04: завели git только
  ради бэкапа на GitHub.

## Changelog
- 2026-06-03 — заведены MEMORY.md и ARCHITECTURE.md; `git save` = обновлять файлы без git.
- 2026-06-03 — источник резюме переведён с PDF на `.md`.
- 2026-06-03 — добавлены правки C-001 (формат), C-002 (прошедшее время), C-003 (живые фразы); письма по «GRI» одобрены.
- 2026-06-04 — заведён `bench/gri/` (пара вакансия+письмо) как первый эталон.
- 2026-06-04 — новая вакансия «Островок» (Supply); сгенерированы письма.
- 2026-06-04 — правки C-004 (понятность постороннему читателю), C-005 (БД vs язык запросов: не «из ClickHouse на SQL»).
- 2026-06-04 — подключён скилл `humanizer` + переключатель `prompt/humanizer.md`; humanized-версия в `outputs/letters_humanized.md`.
- 2026-06-04 — заведена команда `git load` (утреннее восстановление контекста, парная к `git save`).
- 2026-06-04 — пачка вакансий (OSMI, Data Analyst, Три Фаундэйшн, Evrone, Логика Молока, Aston, Senior Product Analyst, Ригла, БЮРО 1440, MarfaTech, MIXIT, CRM Group), все письма humanized.
- 2026-06-04 — правки C-006…C-014 (реквизиты, агентства, анти-галлюцинации, запрет ГНБ, ирония про высшее, современный стиль, «Моя сильная сторона», связность, самостоятельность).
- 2026-06-04 — git-бэкап на GitHub (public), `.gitignore` исключает inputs/jira; `git save` расширен до commit+push.
- 2026-06-04 — папка `external-prompts/` (промпт для ChatGPT на файловой модели + building-rules + конспект гайда GPT-5).
- 2026-06-05 — собран `inputs/facts-digest.md` (дистиллят кейсов с цифрами из резюме+Jira); исключён из git.
- 2026-06-05 — заведён `prompt/rules.md` как банк формулировок (термины + обороты, пометки `[факт]`/`[фон]`).
- 2026-06-05 — добавлен «режим редакции» (`outputs/letters_edit.md`): правка письма в редакторе → `ок` → правила.
- 2026-06-05 — правки C-015 (называть инструменты вакансии, что есть у меня), C-016 (без эпитетов-самопохвалы), C-017 (спокойный тон без бравады), C-018 (не называть инструменты, которых нет в вакансии), C-019 (кейс конкретно: задача→сделал→что выяснил→польза, понятно HR).
- 2026-06-05 — ИЗМЕНЕНО ЯДРО: по умолчанию 2 варианта (было 3) + одна строка времени генерации после писем.
- 2026-06-05 — вакансии: ГК Орбита, СДЭК (экономика лояльности), Verme (BI/упр. отчётность), Dodo Brands (B2C), ЧИЖИК/X5; Самокат (Lead) — пропущен, нет лид-опыта.
- 2026-06-08 — ИЗМЕНЕНО ЯДРО по замечаниям HR: структура письма → 3 чётких сегмента (1. связка с компанией/вакансией ПЕРВЫМ абзацем; 2. навыки под запрос; 3. финал). Начало больше не «представился + сразу job title»: первый абзац — про компанию/вакансию (self-intro вплетён короткой строкой), без дежурной лояльности к бренду. В концовку добавлен вежливый финал: благодарность за время + готовность к диалогу/вопросам (блок «на старте могу взять» сохранён).
- 2026-06-08 — правка C-020 (макс. 2-3 кейса с результатом во 2-м сегменте, приоритет соответствию резюме↔вакансии, чтение за ~1 мин; метрики допустимо одной строкой, не кейс).
- 2026-06-08 — правка C-021 (мост готовности по недостающему навыку: реальный близкий опыт + чистая проф-формулировка «смогу быстро взять в работу»; запрет затычек «это рядом», «без раскачки»).
- 2026-06-08 — Telegram-ник обновлён `@konica1970` → `@konica_f1` (в core.md и C-006).
- 2026-06-08 — вакансии по новой структуре: ЧИЖИК/X5 (перегенерирована), RWB (Wildberries+Russ, LTV/прогноз/отток), Литрес Самиздат (Middle, система метрик + инсайты + A/B).
- 2026-06-09 — правки C-022 (вступление живой цельной фразой, а не телеграфным резюме-обрубком «N лет в продуктовых командах»), C-023 (SQL = доступ/выгрузки из БД, аналитика/поиск закономерностей = Python; не «данные беру на SQL»), C-024 (несколько кейсов во 2-м сегменте — списком с маркерами и вводной строкой, без жирных заголовков-ярлыков), C-025 (маркер списков — короткое тире, не буллет-точка «•»: точки не копируются в форму), C-026 (точный символ — дефис-минус «-» U+002D, не «–»/«—»/«•»).
- 2026-06-09 — вакансии: MAGNIT TECH (продуктовый/веб-аналитик, веб-аналитика мостом), «Где мои дети» (продуктовый, удержание/автономия), Т-Банк Weekend Offer (2 варианта под команды Т-Бизнес / Нефинансовые сервисы), WellnessWealthWay (wellness B2C, англ. B1 честно), Eqvanta (финтех/МФО, сильный матч Power BI+SQL), Okko Growth (эксперименты+унификация дашбордов, Airflow/Superset мостом), X5 Tech (DA, Grafana/Tableau/ClickHouse, pyspark мостом).
- 2026-06-09 — Озон Доставка: заголовок «Продуктовый аналитик», но тело — системный аналитик (СА 80%, UML/BPMN, интеграции). Выведено противоречие; по решению пользователя письмо написано как продуктовому, СА-часть закрыта честной строкой (не выдумывали).
- 2026-06-09 — добавлен звуковой хук: `.claude/settings.local.json` → Stop-хук (PowerShell, WPF MediaPlayer) проигрывает `C:\claude_code_projects\tank.mp3` при свежем `outputs/letters.md` (<90 с после генерации). Файл настроек исключён из git (`.gitignore`). Активируется после `/hooks` или рестарта. Проверено — работает.
- 2026-06-09 — вакансия Sputnik8 (travel, продуктовый аналитик): эксперименты + система метрик + ownership; bootstrap/CUPED/causal и Growthbook/Metabase — честным мостом.
- 2026-06-09 — правки про формулировку кейсов: C-027 (не склеивать сущности; что тестировали → что сделал → результат с ясным подлежащим → решение), C-028 (один пункт списка = одна задача, не сваливать несколько в буллет), C-029 (без пустых слов «инсайт/проактивно/драйвер» как самоописания; не называть метод без бизнес-контекста и не склеивать методы — «когортным анализом юнит-экономики» неверно; живой тон).
- 2026-06-09 — правка C-030 (живой язык «как аналитик сказал бы коллеге на созвоне»: запрет англицизм/канцелярит-конструкций — «полный цикл A/B-теста», «ownership», «единая система уровней», «BI собирал», «глубокие A/B держу на t-test», «data-driven подход»; формула задача→что сделал→что получилось→чем помогло). Письма Sputnik8 переписаны и одобрены пользователем.
- 2026-06-09 — вакансия «Восход» (фарм-дистрибуция, аналитик данных с нуля); 1С/фарма/plotly — честным мостом.
- 2026-06-09 — правки C-031 (вводная перед кейсами — живые варианты, варьировать) и C-032 (каждый кейс привязан к вакансии; не вставлять нерелевантный ради метрики).
- 2026-06-09 — ИЗМЕНЕНО ЯДРО (повышение C-033 до core): новый раздел «РЕЛЕВАНТНОСТЬ КЕЙСОВ (жёстко)» — релевантность важнее объёма, меньше кейсов но релевантных, ничего не выдумывать. Минимумы в «ОБЯЗАТЕЛЬНО В КАЖДОМ ПИСЬМЕ» смягчены: «измеримый результат» и «кейс из финтеха» теперь «если релевантно вакансии» (был конфликт безусловных минимумов с релевантностью).
- 2026-06-09 — вакансия FIX (аналитик по исследованиям, ИТ/финтех): сильный матч через ML/AI-кейсы (LLM-детект обхода площадки ~1,5 млн ₽/мес, NLP тем/тональности, нормативка). Высшее в требованиях → C-010 (ирония + курсы/практика). Первое письмо по обновлённому ядру.
